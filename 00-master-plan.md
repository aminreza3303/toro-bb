# ترب‌بیزینس — برنامهٔ اجرایی پورتفولیو

**وضعیت:** طرح مستقل و غیررسمی؛ فازهای ۰ تا ۳ با دموی محلی قابل‌کلیک آماده‌اند  
**تاریخ:** ۲۰۲۶-۰۹-۱۶  
**مالک تصمیم‌ها:** سازندهٔ پروژه

## هدف و معیار بیرونی

این پروژه یک پیشنهاد پورتفولیویی برای تجربهٔ «تربِ خرید سازمانی» است؛ محصول رسمی یا زیرمجموعهٔ تأییدشدهٔ ترب نیست. بازار آزمایشی، خرید اقلام استاندارد اداری و تجهیزات IT برای کسب‌وکارهای کوچک و متوسط است. هدف کوتاه‌مدت، ساخت تجربه‌ای است که درخواست خرید را به پیشنهادهای قابل‌مقایسه تبدیل کند، پیشنهادهای ناهمگون فروشندگان را یکسان‌سازی کند، با توجه به نیاز خریدار رتبه بدهد و دلیل انتخاب را نشان دهد.

[آگهی AI Product Engineer ترب](https://jobs.torob.com/ai-product-engineer) صریحاً انتخاب یک بازار، گردآوری پیشنهادها، یکسان‌سازی دادهٔ نامرتب، رتبه‌بندی بر اساس قصد کاربر، توضیح بهترین انتخاب و ارائهٔ دموی حداکثر ۵ دقیقه‌ای را می‌خواهد. بنابراین کیفیت این مسیر، معیار اصلی پروژه است. MCP، چندعاملی، مذاکره و خرید خودکار می‌توانند عمق فنی پورتفولیو را نشان دهند، اما در آگهی شرط صریح نیستند و نباید زمان ساخت دمو را مصرف کنند.

## تصمیم‌های محصول

| تصمیم | وضعیت | دلیل |
|---|---|---|
| بازار X: خرید سازمانی اقلام اداری/IT استاندارد | فرض طراحی | کالاهای قابل‌تطبیق و تکرارپذیر، مناسب برای نمایش یکسان‌سازی و رتبه‌بندی‌اند. باید با مصاحبهٔ کاربر یا دادهٔ رفتار واقعی اعتبارسنجی شود. |
| تجربهٔ مستقل با هویت نمونه‌کار | تصمیم برای دمو | از برداشت وابستگی رسمی به ترب جلوگیری می‌کند و آزادی طراحی می‌دهد. دامنهٔ `business.torob.com` فقط ایدهٔ متن اولیه بود، نه دارایی پروژه. |
| MVP: RFQ دستی، دادهٔ محدود، تطبیق و رتبه‌بندی توضیح‌پذیر | تصمیم فاز ۰ | مسیر اصلی چالش رسمی را در یک دمو می‌بندد. |
| فاکتور و زمان تحویل | ویژگی داده/فیلتر با وضعیت «اظهارشده» | تا وقتی راستی‌آزمایی واقعی نداریم، سیستم نباید تضمین نمایش دهد. امتیاز اعتماد فروشنده به Growth موکول است. |
| پرداخت/تسویه و سفارش‌گذاری واقعی | موکول | نیازمند قرارداد فروشنده، پشتیبانی، فرایند مالی و سیاست بازگشت است؛ برای اثبات هستهٔ انتخاب لازم نیست. |
| دسترسی به دادهٔ اختصاصی ترب | فرضِ فاقد دسترسی | دمو با دادهٔ دارای منبع و زمان ثبت یا fixture ساختگیِ آشکار کار می‌کند؛ مزیت بنچمارک سراسری تنها در صورت دسترسی مجاز قابل ادعاست. |

## فازها و گیت‌های خروج

| فاز | خروجی قابل‌نمایش | گیت خروج |
|---|---|---|
| ۰. تعریف محصول | `01-product-spec.md` و `06-feature-backlog.md` | یک سناریوی اصلی و معیار پذیرش اندازه‌پذیر، بدون ادعای تأییدنشده |
| ۱. معماری | `02-architecture.md` | مدل داده و قرارداد API، مسیر منبع→نرمال‌سازی→رتبه‌بندی→توضیح، مرز MCP |
| ۲. هستهٔ تصمیم | `03-ai-agents.md` و اجرای محلی موتور رتبه‌بندی | خروجی تکرارپذیر برای چند مورد مرزی، توضیح متصل به شواهد |
| ۳. ساخت | دموی قابل‌کلیک | کاربر RFQ بسازد و مقایسه و دلیل انتخاب را ببیند |
| ۴. ارزیابی | گزارش QA و سناریوهای خطا | آزمون نرمال‌سازی، پوشش/عدم‌قطعیت و ثبت تصمیم‌ها |
| ۵. گسترش اختیاری | یک قابلیت تکمیلی | فقط اگر کیفیت دموی اصلی حفظ شود |
| ۶. بسته‌بندی | `05-portfolio.md`، README، ویدیو ≤۵ دقیقه | مسئله، داده، تصمیم‌ها، محدودیت‌ها، نقش عملی AI در تحقیق/ساخت/آزمون و لینک‌های در دسترس پروژه و ویدیو روشن باشند |

## ارکستریشن و فعال‌سازی نقش‌ها

نقش‌های Product Strategy، UX/Flow، Architecture/MCP، AI/Agent Design، Build و QA/Eval در بازبینی هر فاز حضور دارند؛ به معنی فرایندهای هم‌زمان دائمی نیست. در فاز ۰ نقش Product Strategy پیش‌نویس اسپک و بک‌لاگ را تولید می‌کند و اورکستریتور ادعاها و تناسب با آگهی را بررسی می‌کند. در فاز ۱ Architecture/MCP وارد طراحی می‌شود؛ QA/Eval معیارهای صحت تطبیق و توضیح را بررسی می‌کند. Trust/Verification، Fintech/Credit و Integrations فقط با ورود فروشنده، پرداخت یا سامانهٔ بیرونی واقعی فعال می‌شوند. Portfolio/Storytelling در فاز ۶ فعال می‌شود. این فعال‌سازی مرحله‌ای، از ساخت زیرساختی که دمو هنوز به آن نیاز ندارد جلوگیری می‌کند.

## ریسک‌ها و پرسش‌های باز

1. **تناسب با چالش:** خرید سازمانی باید در پنج دقیقه به‌عنوان یک «بازار X» قابل‌فهم باشد؛ اگر پیچیدگی فرایند خرید روایت را مبهم کرد، دامنه به مقایسهٔ اقلام تکراری سازمانی محدود می‌شود.
2. **داده:** اعداد «۶ میلیون محصول» و «۱۲ هزار فروشگاه» در متن اولیه این پروژه تأیید نشده‌اند. [صفحهٔ اصلی ترب](https://torob.com/) فقط «میلیون‌ها محصول» و «هزاران فروشگاه» را بیان می‌کند. در اسناد و دمو از عدد دقیق و ادعای دسترسی به دادهٔ ترب استفاده نمی‌شود.
3. **منبع پیشنهادها:** پیش از اتصال خزنده به فروشگاه واقعی باید شرایط استفاده و امکان برداشت داده بررسی شود. fixture ساختگی باید آشکار برچسب‌گذاری شود. برای بستن گیت پیش از ارسال، دست‌کم یک منبع واقعیِ مجاز باید با URL، زمان برداشت، متن خام و ردّ تبدیل وارد شود؛ بدون آن، بخش گردآوری واقعیِ چالش انجام نشده است.
4. **کیفیت انتخاب:** قیمت پایین‌تر به‌تنهایی «بهترین» نیست؛ موجودی، هزینه و زمان ارسال، حداقل سفارش و فاکتور رسمی باید در رتبه‌بندی یا نمایش عدم‌قطعیت حضور داشته باشند.
5. **اعتبار محصول:** مدل درآمد، جایگاه سازمانی مستقل و مزیت رقابتی نسبت به دیجی‌کالا بیزینس هنوز فرضیه‌اند، نه واقعیت بازارِ اثبات‌شده.

## گام بعدی

هستهٔ آفلاین فاز ۲ با fixture ناهمگون، جست‌وجوی RFQ، نرمال‌سازی، رتبه‌بندی و آزمون‌های طلایی ساخته شده است. رابط و API محلی فاز ۳ نیز اجرا شده و مسیر RFQ، تطبیق مبهم، تغییر اولویت، نمایش منشأ، بودجهٔ ناکافی و چیدمان موبایل در مرورگر بررسی شده‌اند. گام بعدی فاز ۴، ارزیابی نظام‌مند حالات خطا و کیفیت تجربه است. نمونهٔ عامل خودمختار و منبع واقعیِ مجاز هنوز اجرا نشده‌اند و باید پیش از ادعای مربوط به آن‌ها ساخته و ارزیابی شوند.
