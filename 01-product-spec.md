# مشخصات محصول «ترب‌بیزینس» — فاز صفر

**وضعیت:** پیش‌نویس تصمیم محصول برای نمونه‌کار؛ ۱۴۰۵/۰۶/۲۵ (۲۰۲۶-۰۹-۱۶)  
**منبع مسئله:** متن ارسالی کاربر، به‌ویژه بخش «خروجی فاز ۰»؛ معیار بیرونی: [آگهی AI Product Engineer ترب](https://jobs.torob.com/ai-product-engineer).  
**مالک تصمیم:** پروژهٔ نمونه‌کار؛ این سند بیانگر برنامه یا محصول رسمی ترب نیست.

## ۱. فرضیه و مرز اعتبار

ایدهٔ مورد آزمایش، یک تجربهٔ مستقل برای تهیهٔ درخواست قیمت سازمانی (RFQ) و پیشنهاد ترکیب تأمین‌کنندگان در بازار X، یعنی **اقلام استاندارد اداری و تجهیزات IT برای کسب‌وکارها**، است. «استقلال» در این نمونه‌کار یک **انتخاب طراحی محصول و روایت** است؛ وجود شرکت، زیرمجموعه، دامنه، همکاری یا دسترسی به دادهٔ ترب را اثبات نمی‌کند. دمو از دادهٔ ساختگی آشکار و/یا نمونهٔ عمومیِ مجاز با منبع مشخص استفاده می‌کند.

آگهی رسمی ترب از متقاضی می‌خواهد بازاری را انتخاب کند، پیشنهادها را گردآوری کند، دادهٔ نامرتب را یکسان‌سازی کند، مطابق قصد کاربر رتبه بدهد، دلیل بهترین انتخاب را نشان دهد و دموی حداکثر پنج دقیقه‌ای ارائه کند. این‌ها **انتظار آگهی** هستند؛ انتخاب بازار خرید سازمانی و طراحی RFQ **پیشنهاد این پروژه** است. آگهی، MCP یا معماری چندعامله را شرط صریح نمی‌گذارد. مسیر اصلی دمو باید «پیشنهاد خام ← دادهٔ یکسان‌شده ← رتبه‌بندی بر اساس نیاز ← دلیل انتخاب» را نشان دهد.

فرضیهٔ ارزش: خریدار می‌تواند به‌جای مقایسهٔ دستی پیشنهادها، برای یک فهرست خرید، چند ترکیب قابل‌اجرا را با قیمت کل و دلیل انتخاب ببیند و دربارهٔ تماس یا مذاکره با فروشندگان تصمیم بگیرد. این فرضیه هنوز با کاربر واقعی اعتبارسنجی نشده است.

در این بازار، **RFQ همان جست‌وجوی ساختاریافتهٔ چندقلمی** است: نام/مشخصات و تعداد اقلام، پرس‌وجو را می‌سازند؛ سامانه برای هر قلم نامزدهای کالا و پیشنهاد فروشنده را نشان می‌دهد؛ خریدار ابهام را رفع و قید/قصد را تنظیم می‌کند؛ سپس ترکیب‌های واجد شرایط رتبه می‌گیرند. دمو باید این مسیر جست‌وجو تا انتخاب را نشان دهد، نه صرفاً فرم درخواست خرید و یک نتیجهٔ نهایی.

| گزارهٔ ورودی که نیاز به راستی‌آزمایی دارد | برخورد در فاز صفر |
| --- | --- |
| شمار کالاها/فروشگاه‌ها و دسترسی به دادهٔ تجمیعی ترب | در ارزش پیشنهادی یا محاسبات دمو استفاده نمی‌شود؛ فقط فرضیهٔ دسترسی آینده است. |
| ساختار حقوقی/محصولی ترب‌بیزینس و رابطه با گروه ترب | محصول مستقل صرفاً کانسپت نمونه‌کار است؛ هیچ وابستگی رسمی ادعا نمی‌شود. |
| قابلیت‌ها و محدودیت‌های دیجی‌کالا بیزینس یا Amazon Business | جدول برتری رقابتی یا ادعای «غیرقابل‌کپی» بدون منبع منتشر نمی‌شود. |
| ضرورت MCP و چندعاملی برای این نقش | در [آگهی](https://jobs.torob.com/ai-product-engineer) شرط صریح نیست؛ ایدهٔ تکمیلی پورتفولیو است و از مسیر اصلی دمو اولویت کمتری دارد. |

## ۲. کاربر و مسئله

**پرسونای اصلی — سارا، کارشناس/مدیر خرید یک سازمان متوسط (فرضیهٔ پژوهشی).** یک فهرست چندقلمی می‌گیرد، قیمت و شرایط را از چند فروشنده گرد می‌آورد و باید دلیل انتخاب را به همکاران توضیح دهد. در MVP او تنها نقش تعاملی است؛ تعداد کارکنان و فراوانی این درد هنوز دادهٔ تحقیقاتی ندارد.

**پرسونای ثانویه — امیر، مدیر عملیات کسب‌وکار کوچک (فرضیه).** خودش درخواست را ثبت و گزینه‌ها را بررسی می‌کند. برای دمو از همان جریان تک‌نقشی استفاده می‌کند.

**پرسونای آینده — نگار، مسئول مالی/تأییدکنندهٔ بودجه (فرضیه).** به فاکتور و شواهد هزینه نیاز دارد. گردش تأیید و ورود مستقل او خارج از MVP است.

### سه کار اصلی کاربر (JTBD)

1. وقتی فهرست خرید عمده دارم، می‌خواهم گزینه‌های قابل‌مقایسه و دلیل پیشنهاد هر کدام را ببینم تا زمان غربال اولیه کم شود. **مستقیماً در MVP آزموده می‌شود.**
2. وقتی خرید تکراری دارم، می‌خواهم با قانون مصوب خودم پیش‌نویس یا اجرای کنترل‌شده داشته باشم تا کار ماهانه کم شود. **نیاز آینده؛ اجرای خودکار در MVP نیست.**
3. وقتی هزینه را گزارش می‌کنم، می‌خواهم مبنای قابل‌پیگیری برای مقایسه و صرفه‌جویی داشته باشم. **در MVP فقط مقایسهٔ پیشنهادهای محدود همان درخواست؛ بنچمارک بازار واقعی در آینده و منوط به دادهٔ معتبر است.**

## ۳. خروجی مورد انتظار MVP

یک دمو قابل‌اجرا که خریدار از ویترین جست‌وجوپذیر کالا شروع می‌کند، جزئیات پیشنهادها را می‌بیند و با انتخاب کالا یا ورود دستی، RFQ چندقلمی می‌سازد؛ سامانه پیشنهادهای خام و ناهمگون را از **snapshotهای محلی قابل‌بازتولید** یا نمونهٔ عمومی/دارای مجوز وارد و نرمال می‌کند؛ سپس از میان پیشنهادها در **دو دستهٔ کالا و ۵ تا ۱۰ فروشندهٔ ساختگی** چند ترکیب تأمین می‌سازد. خریدار جمع هزینه، پوشش اقلام، قیود برآورده‌شده/نشده، منبع و زمان ثبت داده، و علت رتبه را می‌بیند. خروجی **پیشنهاد برای بررسی انسانی** است، نه سفارش، رزرو قیمت یا تضمین موجودی. خزیدن زنده و گستردهٔ وب در این MVP نیست.

### جریان اصلی

1. خریدار در ویترین، کالاها را جست‌وجو یا با دسته‌بندی فیلتر می‌کند؛ جزئیات پیشنهادهای پذیرفته‌شده را می‌بیند و کالاهای موردنیاز را به فهرست اضافه می‌کند. مسیر ورود بدون حساب و تک‌نقشی است.
2. برای هر قلم، نام/مشخصات و تعداد را از کاتالوگ انتخاب یا دستی وارد می‌کند؛ نامزدهای کاتالوگ و پیشنهادهای قابل‌جست‌وجو را می‌بیند. در صورت وجود، سقف بودجه، حداکثر زمان تحویل و فیلتر «فاکتور طبق اظهار فروشنده» را ثبت می‌کند؛ دمو امکان درخواست فاکتور *تأییدشده* ندارد.
3. یک فرایند واردسازی، پیشنهادهای خام را از snapshotهای منبع می‌خواند و شکل‌های ناهمگون قیمت، واحد، نام کالا و شرایط را به رکورد یکسان‌شده تبدیل می‌کند؛ رکورد خطادار با دلیل کنار گذاشته می‌شود و متن خام و منشأ آن حفظ می‌شود.
4. سیستم اقلام RFQ را با کاتالوگ محدود تطبیق می‌دهد؛ موارد مبهم یا بی‌تطابق را برای اصلاح نشان می‌دهد و تطبیق را بی‌صدا حدس نمی‌زند.
5. سیستم پیشنهادهای واجد شرایط را می‌گیرد، قیود سخت را اعمال می‌کند و ترکیب‌ها را بر اساس **قصد صریح خریدار** (مانند کمترین هزینه در سقف زمان، یا تحویل سریع در سقف بودجه) رتبه می‌دهد.
6. خریدار جدول مقایسه و توضیح هر گزینه را می‌بیند: اقلام و فروشنده‌ها، قیمت واحد، هزینهٔ هر قلم و جمع، زمان تحویل و فاکتور **طبق دادهٔ ثبت‌شده**، منبع/زمان snapshot، و دلایل رد/افت رتبه. می‌تواند به فرم برگردد و درخواست را اصلاح کند.

### الزامات رفتاری و معیار پذیرش

| شناسه | الزام | معیار پذیرش قابل‌بررسی |
| --- | --- | --- |
| M0 | ویترین خرید سازمانی | شروع در صفحهٔ کالا، جست‌وجو/دسته‌بندی و نمایش قیمت بسته و تعداد پیشنهاد پذیرفته‌شده؛ جزئیات هر کالا فقط پیشنهادهای پذیرفته‌شده را با فروشنده، بسته‌بندی، موجودی، تحویل و منشأ نشان می‌دهد؛ افزودن به فهرست، قلم و تعداد را به RFQ منتقل می‌کند. صفحهٔ معرفی کسب‌وکار، مسیرهای ثبت شرکت و پروانهٔ کسب را صرفاً به‌عنوان برنامهٔ محصول بیان می‌کند. |
| M1 | ورود دستی RFQ | فرم حداقل دو قلم را می‌پذیرد؛ تعداد نامعتبر/صفر، قلم خالی و واحد ناسازگار را با خطای قابل‌فهم رد می‌کند؛ کاربر می‌تواند قلم را ویرایش/حذف کند. |
| M2 | دادهٔ شفاف دمو | مجموعهٔ ثابت شامل دقیقاً دو دسته و ۵ تا ۱۰ فروشندهٔ ساختگی است؛ در صفحهٔ ورود و نتایج، ساختگی‌بودن فروشنده، قیمت، زمان تحویل و فاکتور آشکار است؛ تاریخ/نسخهٔ snapshot در نتایج دیده می‌شود. |
| M2a | گردآوری و واردسازی قابل‌ممیزی | حداقل دو سند/صفحهٔ پیشنهاد خام با ساختار متفاوت از snapshotهای محلی وارد می‌شود؛ برای هر رکورد، `source_id`، نشانی یا شناسهٔ سند، متن خام و `generated_at` برای دادهٔ ساختگی یا `observed_at` برای دادهٔ واقعی قابل‌ردیابی است. پیش از ارسال دمو دست‌کم یک نمونهٔ واقعیِ مجاز با همین مسیر وارد می‌شود؛ بدون بررسی شرایط استفاده، خزنده به فروشگاه واقعی وصل نمی‌شود. |
| M2b | نرمال‌سازی دادهٔ نامرتب | fixture حداقل تفاوت رقم فارسی/لاتین، تومان/ریال، جداکنندهٔ قیمت، واحد بسته/عدد و نام‌گذاری متفاوت یک قلم را دارد؛ خروجی واحد/ارز پایه و قیمت واحد قابل‌مقایسه تولید می‌کند. مقدار مبهم یا تبدیل‌ناپذیر با علت قرنطینه می‌شود؛ خام و تبدیل‌شده کنار هم قابل‌دیدن‌اند. |
| M3 | تطبیق محتاطانه | هر قلم به گزینهٔ مشخص کاتالوگ وصل می‌شود یا وضعیت «نیازمند اصلاح/بدون تطبیق» می‌گیرد؛ سیستم برای قلم بی‌تطابق قیمت یا فروشنده اختراع نمی‌کند. |
| M4 | پیشنهاد قابل‌ممیزی | هزینه بر اساس **تعداد بسته/گام واقعاً قابل‌خرید**، حداقل سفارش و هزینهٔ ارسال یک‌بارهٔ هر فروشنده حساب می‌شود؛ اضافه‌خرید و جمع هر فروشنده دیده می‌شود. هر قلم به شناسهٔ پیشنهاد و سند خام منبع قابل‌ردیابی است؛ اجرای دوبارهٔ همان ورودی روی یک snapshot، خروجی و ترتیب یکسان می‌دهد. |
| M5 | قیود و پوشش | پیشنهاد ناقض قید سخت یا دارای مقدار نامعلوم در فیلد لازم (موجودی، تحویل، هزینهٔ ارسال، حداقل سفارش یا ادعای فاکتور در صورت درخواست) به‌عنوان «واجد شرایط» نمایش داده نمی‌شود؛ علت و وضعیت `نامعلوم/اظهارشده/تأییدشده` متناسب با منبع دیده می‌شود. فاکتور فروشنده در دمو فقط «اظهارشده» است. اگر پوشش کامل ممکن نیست، اقلام فاقد پوشش و علت نشان داده می‌شوند و ادعای «بهترین ترکیب کامل» نمی‌شود. |
| M6 | قصد کاربر و توضیح تصمیم | دست‌کم دو قصد ازپیش‌تعریف‌شده (کمترین هزینه با قید زمان؛ سریع‌ترین تحویل با قید بودجه) قابل‌انتخاب‌اند و fixture موردی دارد که انتخاب برتر میان آن‌ها تغییر کند؛ برای حداقل دو گزینهٔ واجد شرایط، دلیل رتبه به داده و ترجیح انتخاب‌شده ارجاع می‌دهد. متن توضیح ادعای تأیید فروشنده، قیمت زنده یا صرفه‌جویی بازار نمی‌کند. |
| M7 | نقش و اختیار | تنها نقش «خریدار» در دمو وجود دارد؛ هیچ دکمه‌ای خرید، تماس خودکار، مذاکره، پرداخت یا تأیید مالی را اجرا نمی‌کند. |
| M8 | روایت دمو | مسیر منبع خام → نرمال‌سازی → ثبت RFQ → رتبه‌بندی با دو قصد → توضیح انتخاب و محدودیت داده در ویدیویی حداکثر پنج دقیقه قابل‌نمایش است. |

معیار کیفیت برای گیت دمو: سناریوهای ازپیش‌تعریف‌شده شامل تبدیل واحد/ارز، رکورد خام مبهم، پوشش کامل، تساوی قیمت، نبود پیشنهاد، نقض قید و قلم مبهم باید نتیجهٔ قابل‌پیش‌بینی و توضیح سازگار با محاسبات بدهند. نمونه‌های طلایی پایین بخشی از oracle ارزیابی‌اند؛ مجموعهٔ کامل fixture در فاز QA/Eval تکمیل می‌شود. این سند ادعای عملکرد واقعی یا صرفه‌جویی عددی ندارد. **شکاف صریح با آگهی:** snapshot محلیِ ساختگی، توان واردسازی را نشان می‌دهد اما «crawl offers» واقعی را اثبات نمی‌کند. پیش از ارسال درخواست، حداقل یک منبع واقعیِ مجاز باید با نشانی، زمان برداشت، متن خام و خروجی تبدیل‌شده از همین مسیر عبور کند. اگر این گیت انجام نشد، جزء گردآوری واقعیِ چالش محقق نشده و این محدودیت باید در ویدیو و README صریح باشد.

### نمونه‌های طلایی برای ارزیابی

| ورودی/سناریو | خروجی مورد انتظار |
| --- | --- |
| پیشنهاد `PACK`: «بستهٔ ۱۰ عددی، ۱۰۰٬۰۰۰ ریال با مالیات»؛ RFQ برای ۱۳ عدد؛ `min_packages=1`, `package_step=1`, موجودی ۲ بسته؛ حداقل سفارش فروشنده ۰، ارسال معلوم ۲۰٬۰۰۰ ریال، تحویل معلوم ۲ روز و بدون فیلتر فاکتور | خرید ۲ بسته، پوشش ۲۰ عدد، اضافه‌خرید ۷ عدد، هزینهٔ کالا ۲۰۰٬۰۰۰ و جمع با ارسال **۲۲۰٬۰۰۰ ریال**؛ محاسبهٔ `۱۳ × ۱۰٬۰۰۰` مردود است. |
| دو ترکیب کامل و واجد شرایط `A` و `B`: `A` با جمع نهایی ۲۲۰٬۰۰۰ ریال/۴ روز، `B` با ۲۴۰٬۰۰۰ ریال/۱ روز؛ بودجه و سقف زمان هر دو را مجاز می‌کنند | با قصد «کمترین هزینه»، ترتیب `A,B` و دلیل تفاوت ۲۰٬۰۰۰ ریال؛ با قصد «سریع‌ترین تحویل»، ترتیب `B,A` و دلیل تفاوت ۳ روز. |
| دو ترکیب با هزینه، زمان و شمار فروشندهٔ برابر؛ شناسهٔ پیشنهادهای یکی `offer-01` و دیگری `offer-02` | `offer-01` اول می‌شود و ترتیب در اجرای تکراری روی snapshot یکسان تغییر نمی‌کند. |
| زمان تحویل یکی از پیشنهادها نامعلوم است و کاربر سقف زمان تعیین کرده | پیشنهاد از واجدان شرایط کنار می‌رود و کد دلیل `UNKNOWN_LEAD_TIME` دیده می‌شود؛ مقدار نامعلوم به صفر روز تبدیل نمی‌شود. |
| زمان تحویل یکی از پیشنهادها نامعلوم است و کاربر سقف زمان تعیین نکرده | چون هر دو نوع رتبه‌بندی از زمان تحویل استفاده می‌کنند، پیشنهاد باز هم از رتبه‌بندی کنار می‌رود و `UNKNOWN_LEAD_TIME` دیده می‌شود. |

### «AI Sourcing Agent» در این نسخه چه می‌کند؟

«AI Sourcing Agent» فعلاً نام ایده است؛ هستهٔ الزام‌آور MVP **موتور قاعده‌محور تطبیق و رتبه‌بندی** است. مدل می‌تواند نامزدهای نرمال‌سازی/تطبیق یا جمله‌بندی دلیل را پیشنهاد دهد، ولی تصمیم واجد شرایط بودن و جمع هزینه از داده و قواعد بازتولیدپذیر می‌آید. اگر مدل زبانی پیاده‌سازی شد، ورودی آن فقط رکوردهای دارای منشأ و خروجی آن schema محدود همراه شناسهٔ شاهد است؛ مورد مبهم برای تأیید انسانی می‌رود و خطاهای آن با fixture طلایی سنجیده می‌شود. تا پیاده‌سازی و ارزیابی آن، دمو نباید ادعای داشتن عامل AI عملیاتی کند.

## ۴. مرز ساخت و تعویق

| الان می‌سازیم | چرا |
| --- | --- |
| RFQ دستی؛ واردسازی snapshotهای خام کوچک و نرمال‌سازی؛ دیتاست mock محدود؛ رتبه‌بندی با قصد خریدار و مقایسهٔ قابل‌توضیح؛ نقش خریدار واحد؛ برچسب منبع/نسخهٔ داده | کمترین جریان کامل مطابق هستهٔ چالش رسمی، بدون وابستگی به فروشنده یا تراکنش واقعی. |

| موکول می‌کنیم | شرط بازگشت |
| --- | --- |
| CSV/OCR و ورود از عکس | پس از مشاهدهٔ خطاهای ورود دستی و نیاز واقعی به واردکردن فهرست بلند. |
| checkout چندفروشنده‌ای، پرداخت و سفارش واقعی | قرارداد عملیاتی با فروشنده، قیمت/موجودی معتبر، تسویه و پشتیبانی. |
| چندنقشی و گردش تأیید | اعتبارسنجی نیاز با تیم‌های خرید و مالی و تعریف اختیارات واقعی. |
| امتیاز اعتماد فروشنده و بنچمارک بازار واقعی | دادهٔ قابل‌استناد، مجوز استفاده و روش محاسبهٔ معتبر. |
| خزیدن پیوستهٔ چند فروشگاه، پایش قیمت زنده و پوشش بازار | انتخاب منبع مجاز، طراحی نرخ درخواست، کیفیت داده و عملیات پایش؛ برای اثبات مسیر اصلی، واردسازی کوچک کافی است. |
| خرید خودکار، مذاکره، اعتبار، اتصال حسابداری و MCP بیرونی | ارزش‌سنجی مرحله‌ای، سیاست‌های اختیار/امنیت و زیرساخت مناسب. طراحی معماری MCP در فاز ۱ می‌تواند انجام شود، اما محصول عمومی آن جزو MVP نیست. |

## ۵. فرض‌های لازم برای تصمیم بعدی

- با ۵ گفت‌وگوی اکتشافی با خریداران هدف، مشخص شود که مشکل غالب، جمع‌کردن پیشنهادهاست یا مقایسه و توجیه تصمیم؛ این عدد **برنامهٔ پژوهش** است، نه نتیجه.
- معلوم شود چه فیلدهایی برای تصمیم خرید واقعاً قید سخت‌اند (زمان، فاکتور، حداقل مقدار، شهر تحویل، اعتبار فروشنده). دادهٔ mock نباید این موارد را به‌عنوان واقعیت بازار بازنمایی کند.
- پیش از هر استفاده از داده یا نام تجاری واقعی، مالکیت، مجوز، API و امکان نمایش در نمونه‌کار بررسی شود.
- پس از گیت فاز صفر، فاز ۱ مدل داده و قرارداد API را براساس مسیر منبع خام → نرمال‌سازی → رتبه‌بندی → توضیح مشخص می‌کند؛ طراحی MCP می‌تواند به‌عنوان ضمیمهٔ فنی کم‌اولویت بررسی شود.
