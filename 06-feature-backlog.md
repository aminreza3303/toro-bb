# بک‌لاگ قابلیت‌ها — فاز صفر

**وضعیت:** اولویت‌بندی اولیهٔ نمونه‌کار، ۱۴۰۵/۰۶/۲۵ (۲۰۲۶-۰۹-۱۶).  
**مبنای دامنه:** [مشخصات محصول](01-product-spec.md)، بخش «خروجی فاز ۰» متن ارسالی کاربر و [آگهی AI Product Engineer ترب](https://jobs.torob.com/ai-product-engineer). آگهی، انتخاب بازار X و مسیر گردآوری پیشنهاد → یکسان‌سازی دادهٔ نامرتب → رتبه‌بندی بر اساس قصد کاربر → توضیح انتخاب → دموی حداکثر پنج دقیقه را می‌خواهد. خرید سازمانی، RFQ و دسته‌های اداری/IT انتخاب‌های **این پروژه** هستند. «ترب‌بیزینس» کانسپت است؛ دسترسی به دادهٔ ترب، همکاری سازمانی و مزیت رقابتی ثابت‌شده فرض نمی‌شود.

**معنای لایه‌ها:** Core/MVP برای دمو لازم است؛ Core/بعدی پس از اثبات جریان اصلی می‌آید؛ Growth به کاربر یا دادهٔ واقعی وابسته است؛ Moonshot نیازمند اعتبارسنجی ارزش و کنترل اختیار عامل است. ترتیب جدول، تعهد زمانی نیست.

## Core — دمو و ادامهٔ نزدیک

| ID | قابلیت و تصمیم | تعریف انجام‌شدن / معیار پذیرش | دلیل اولویت یا تعویق |
| --- | --- | --- | --- |
| C01 | **MVP — RFQ Builder دستی** | خریدار حداقل دو قلم با مقدار/واحد، قید و قصد خرید می‌سازد، ویرایش می‌کند و خطاهای ورودی را می‌بیند؛ هیچ واردسازی فایل لازم نیست. | نقطهٔ شروع آزمون تطبیق و رتبه‌بندی؛ اجرای کم‌وابستگی. |
| C02 | **MVP — گردآوری/واردسازی پیشنهاد خام** | حداقل دو صفحه/سند خامِ ناهمگون از snapshot محلی قابل‌بازتولید خوانده می‌شود؛ هر پیشنهاد شناسهٔ منبع، زمان ثبت و متن خام دارد. پیش از ارسال دمو، دست‌کم یک منبع واقعیِ مجاز با همان مسیر و ردّ برداشت افزوده می‌شود. | «گردآوری پیشنهاد» بخش صریح چالش است؛ snapshot ساختگی توان واردسازی را نشان می‌دهد ولی جای crawl واقعی را نمی‌گیرد. |
| C03 | **MVP — نرمال‌سازی + دیتاست نمایشی** | تبدیل رقم فارسی/لاتین، تومان/ریال، واحد بسته/عدد و نام متفاوت یک کالا به قالب مشترک؛ موارد مبهم با دلیل قرنطینه؛ مجموعهٔ ثابت در دو دسته و ۵ تا ۱۰ فروشندهٔ ساختگی با برچسب آشکار. | هستهٔ دادهٔ نامرتب آگهی و پایهٔ مقایسهٔ درست؛ خروجی تکرارپذیر. |
| C04 | **MVP — تطبیق و رتبه‌بندی بر اساس قصد** | تطبیق قابل‌تأیید، اعمال قیود سخت، محاسبهٔ بسته‌های واقعاً قابل‌خرید، اضافه‌خرید و ارسال یک‌بارهٔ هر فروشنده؛ دو قصد «کمترین هزینه با قید زمان» و «سریع‌ترین تحویل با قید بودجه» روی fixture، برندهٔ متفاوت ایجاد می‌کنند؛ نبود پوشش آشکار است. | ارزش محصول و تطابق مستقیم با درخواست رتبه‌بندی بر اساس نیاز کاربر. |
| C05 | **MVP — مقایسه و توضیح پیشنهاد** | حداقل دو گزینهٔ واجد شرایط با ریز اقلام، فروشنده، هزینهٔ کل، منبع خام و دلیل رتبه؛ دلیل به داده و قصد انتخاب‌شده ارجاع می‌دهد و ادعای قیمت زنده نمی‌کند. | تصمیم قابل‌فهم و قابل‌ممیزی؛ هستهٔ UX دمو. |
| C06 | **MVP — حساب/نشست خریدار واحد** | فقط نقش خریدار قابل‌ورود است؛ دسترسی به RFQ/نتایج نمایشی دارد؛ هیچ مجوز تأیید یا اجرای خرید وجود ندارد. | دامنهٔ UX و امنیت را متناسب با دمو نگه می‌دارد. |
| C07 | **MVP — حالات خطا و منشأ داده** | نبود پوشش، قلم مبهم، قیمت ناموجود، تبدیل نامطمئن و نقض قید در UI صریح‌اند؛ شناسهٔ پیشنهاد، سند خام و نسخهٔ داده در نتیجه حفظ می‌شود. | از خروجی ظاهراً قانع‌کننده ولی غلط جلوگیری می‌کند. |
| C08 | **Core/بعدی — ورود CSV** | با نمونه‌فایل تعریف‌شده، پیش‌نمایش و اصلاح ردیف‌ها پیش از ساخت RFQ؛ ردیف نامعتبر جداگانه گزارش می‌شود. | پس از مشاهدهٔ نیاز به ورود فهرست‌های طولانی. |
| C09 | **Core/بعدی — مقایسهٔ سناریو** | خریدار می‌تواند دو تنظیم قید/ترجیح را کنار هم ببیند و تفاوت هزینه و پوشش را توضیح دهد. | ایدهٔ افزوده برای آزمون trade-off؛ پس از پایدارشدن رتبه‌بندی پایه. |

## Growth — پس از داده و عملیات واقعی

| ID | قابلیت | شرط شروع و معیار پذیرش سطح محصول | چرا اکنون موکول است |
| --- | --- | --- | --- |
| G01 | ورودی عکس/OCR | نمونهٔ درخواست‌های واقعی و معیار دقت استخراج؛ کاربر پیش از ثبت همهٔ اقلام استخراج‌شده را تأیید کند. | کیفیت تصویر/زبان و هزینهٔ خطا نامعلوم است. |
| G02 | سبد و checkout چندفروشنده‌ای | توافق با فروشندگان، موجودی/قیمت معتبر، تفکیک سفارش و مسیر پرداخت/پشتیبانی؛ سفارش فقط پس از تأیید خریدار ثبت شود. | زیرساخت تراکنش و تعهد بیرونی می‌خواهد. |
| G03 | حساب سازمانی چندنقشی و approval workflow | نقش‌های خریدار، تأییدکننده و مدیر با ماتریس اختیار؛ رویدادهای تأیید قابل‌ردیابی. | نیاز و سیاست سازمانی هنوز سنجیده نشده است. |
| G04 | Vendor Trust Score و onboarding واقعی | منابع امتیاز، رضایت فروشنده و روش اعتراض/اصلاح روشن؛ امتیاز بدون دادهٔ کافی نمایش داده نشود. | داده و احراز فروشنده نداریم؛ امتیاز ساختگی گمراه‌کننده است. |
| G05 | داشبورد هزینه و بنچمارک بازار | فقط با دسترسی مجاز، نمونهٔ نماینده و روش محاسبهٔ قابل‌ممیزی؛ بازه/پوشش داده کنار عدد نشان داده شود. | «قیمت کل بازار» و درصد صرفه‌جویی در دادهٔ mock قابل اثبات نیست. |
| G06 | سفارش تکرارشونده و یادآور | کاربر دوره و اقلام را تعیین کند؛ در نسخهٔ نخست فقط پیش‌نویس/یادآوری با تأیید دستی. | قبل از خودکارسازی، رفتار خرید واقعی و خطاها باید شناخته شود. |
| G07 | قرارداد قیمت ثابت | تعریف SKU، مدت، مقدار، تعهد و طرف قرارداد با تأیید حقوقی/تجاری. | نیازمند توافق فروشنده و قابلیت اجرای قرارداد است. |
| G08 | اتصال حسابداری/ERP ایرانی | انتخاب یک سیستم بر اساس نیاز کاربر؛ تبادل فاکتور آزمایشی با نگاشت فیلد و مدیریت خطا. | ارزش و قرارداد API هنوز معلوم نیست. |
| G09 | اعتبار/پرداخت مدت‌دار سازمانی | شریک مالی، قواعد اعتبار و رضایت/افشای شرایط؛ تصمیم اعتباری خارج از عامل پیشنهاددهنده. | وابستگی مالی و ریسک عملیاتی بالا است. |
| G10 | توسعهٔ دسته/ورتیکال | انتخاب دستهٔ بعدی پس از مشاهدهٔ تقاضا و سنجش کیفیت داده؛ معیار پوشش برای آن دسته تعریف شود. | افزودن دسته پیش از کیفیت دو دستهٔ نخست، دمو را پراکنده می‌کند. |
| G11 | خزیدن پیوسته و پایش پیشنهادهای واقعی | منبع و شرایط استفاده بررسی شود؛ نرخ درخواست، خطا، تازگی داده و تغییر قالب منبع پایش شوند. | MVP فقط برش کوچک و قابل‌بازتولید واردسازی را لازم دارد؛ اتصال عملیاتی به فروشگاه واقعی وابستگی‌های بیشتری دارد. |

## Moonshot — آزمایش‌های عامل‌محور، نه وعدهٔ MVP

| ID | قابلیت | حداقل کنترل و گیت ارزش | دلیل تعویق |
| --- | --- | --- | --- |
| M01 | Autonomous Procurement Agent | سیاست مصوب با سقف مبلغ/دوره، فروشندهٔ مجاز، توقف اضطراری، تأیید انسانی برای استثنا و audit trail کامل؛ ابتدا در محیط شبیه‌سازی. | خرید واقعی بدون داده، مجوز و کنترل خسارت قابل‌قبول نیست. |
| M02 | Negotiation Agent / A2A | مذاکره فقط با طرف‌های رضایت‌داده و محدودهٔ پیشنهاد مصوب؛ ثبت مکاتبات و تأیید انسان پیش از تعهد. | پروتکل، اختیار نمایندگی و پاسخ‌گویی حل نشده‌اند. |
| M03 | Demand Forecasting Agent | روی تاریخچهٔ مجاز، پیش‌بینی همراه عدم‌قطعیت و تبدیل صرفاً به پیش‌نویس RFQ. | تاریخچهٔ واقعی و سنجش خطای پیش‌بینی نداریم. |
| M04 | Supplier Risk Agent | شاخص‌های تعریف‌شده، شواهد و هشدار قابل‌اعتراض؛ آزمون نرخ هشدار غلط پیش از استفادهٔ عملیاتی. | دادهٔ تأخیر/شکایت معتبر و حق استفاده موجود نیست. |
| M05 | Market Intelligence API / MCP بیرونی | مجوز داده، کنترل دسترسی، حداقل‌سازی و نسخه‌بندی قرارداد؛ خروجی نمونه بدون اطلاعات اختصاصی یا شخصی. | طراحی MCP داخلی در فاز ۱ مفید است؛ عرضهٔ بیرونی به حقوق داده و عملیات نیاز دارد. |

## گیت بازنگری و تصمیم build/defer

1. **پایان فاز صفر:** فقط C01–C07 در تعریف MVP باقی می‌مانند. اگر دو دسته/دادهٔ mock نتوانند تبدیل ناهمگون، پوشش کامل و نبود پوشش را نشان دهند، fixture اصلاح شود؛ دامنهٔ محصول بزرگ‌تر نشود.
2. **پایان فاز AI Core:** پیشنهاد باید روی snapshot ثابت بازتولیدپذیر باشد و توضیح با محاسبات و قصد خریدار بخواند. نمونهٔ آزمایشی عامل خودمختار/مذاکره، اگر ساخته شود، فقط شبیه‌سازی جدا از MVP و با policy و audit trail است؛ آگهی آن را شرط صریح نکرده است.
3. **پیش از Growth:** با کاربر واقعی مسئله و ترتیب دردها تأیید شود؛ هر ادعای دسترسی به داده یا قابلیت رقبا از منبع معتبر بررسی شود؛ برای قابلیت تراکنشی، قرارداد و عملیات لازم تعریف شود.
4. **پیش از ارسال دمو:** مسیر پیشنهاد خام تا توضیح انتخاب در حداکثر پنج دقیقه نشان داده شود. دست‌کم یک منبع واقعیِ مجاز با URL، زمان برداشت، متن خام و ردّ تبدیل از مسیر بگذرد. اگر این گیت محقق نشد، بخش «crawl offers» چالش **انجام‌نشده** است و محدودیت در README و ویدیو صریح گفته می‌شود.
5. **پیش از Moonshot:** ارزش نسبت به جریان دستی، مرز اختیار، امکان توقف و معیار ارزیابی خطا مستند و توسط QA/Eval تأیید شود.

**ایدهٔ تازهٔ فاز صفر:** C07 (منشأ داده و حالات نامطمئن) و C09 (مقایسهٔ سناریو). این‌ها به ترتیب صداقت دمو و فهم trade-off خریدار را تقویت می‌کنند؛ C09 خارج از MVP است.
