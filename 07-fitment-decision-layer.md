# لایهٔ تصمیم کالا — سازگاری، تفاوت قیمت و اعتماد

**وضعیت:** فاز ۱ پیاده‌سازی‌شده در ترب‌بیزینس؛ vertical قطعات خودرو در فاز بعدی  
**نوع تغییر:** ادامهٔ پروژهٔ موجود، بدون جایگزینی جریان فعلی  
**تاریخ:** ۱۴۰۵/۰۶/۲۶ (۲۰۲۶-۰۹-۱۷)  
**ارتباط:** [مشخصات محصول](01-product-spec.md)، [معماری](02-architecture.md)، [بک‌لاگ](06-feature-backlog.md)

## ۱. خلاصهٔ اجرایی

ترب‌بیزینس نباید فقط قیمت چند کالا یا فروشنده را کنار هم بگذارد. در کالاهای پرابهام، کاربر ابتدا می‌خواهد بداند **کدام گزینه اصلاً برای نیازش مناسب است**، بعد می‌خواهد اختلاف قیمت را بفهمد و در نهایت فروشنده را انتخاب کند.

این سند یک لایهٔ اختیاری به محصول اضافه می‌کند:

```text
نیاز/وسیلهٔ کاربر → گزینهٔ سازگار → توضیح تفاوت گزینه‌ها → شواهد اعتماد فروشنده → اقدام خرید
```

این الگو برای قطعات خودرو به‌شکل `vehicle fitment` (تناسب با خودرو) دیده می‌شود؛ برای دسته‌های فعلی ترب‌بیزینس نیز کاربرد دارد: سازگاری تونر با پرینتر، اندازهٔ کاغذ، نوع اتصال مانیتور، مدل کیبورد یا نیاز کاربر به صندلی اداری.

این قابلیت به معنی ادعای اتصال رسمی به ترب یا دادهٔ زنده نیست. در این پروژه، هر ادعا باید با منبع، زمان و وضعیت اطمینان خود نمایش داده شود.

### آنچه اکنون در دمو فعال است

- پنج زمینهٔ انتخاب برای ذخیره‌سازی، مانیتور، صندلی اداری، لوازم جانبی و کاغذ در snapshot محلی ثبت شده است.
- `GET /api/catalog/decision?q=...`، `GET /api/catalog/decision/{context_id}` و `POST /api/catalog/decision/{context_id}/answers` قابل استفاده‌اند.
- صفحهٔ فهرست، «جواب کالا» را قبل از کارت‌های قیمت نشان می‌دهد؛ صفحهٔ جزئیات، claimها، فرق variantها و شواهد منشأ قیمت را کنار لیست تأمین‌کننده نمایش می‌دهد.
- پاسخ انتخابی کاربر همراه قلم به RFQ فعلی منتقل می‌شود؛ با تغییر دستی عبارت، زمینهٔ قبلی پاک می‌شود.

## ۲. مبنا و تغییر دقیق

### وضعیت فعلی

- مسیر فعلی از ویترین کالا به جزئیات محصول، قیمت‌های تأمین‌کننده و RFQ چندقلمی می‌رود.
- جدول‌های `products`, `suppliers` و `product_prices` برای نمایش قیمت و منشأ وجود دارند.
- تطبیق فعلی عمدتاً بر اساس نام/alias و مشخصات محدود انجام می‌شود.
- دادهٔ فعلی snapshot محلی است و بسیاری از فیلدهای تجاری می‌توانند نامعلوم باشند.

### تغییر پیشنهادی

- `product` از یک عنوان و چند spec به یک **گزینهٔ قابل‌تصمیم** با ادعاهای قابل‌ردیابی تبدیل می‌شود.
- قبل از مقایسهٔ فروشنده، سیستم قابل‌مقایسه‌بودن گزینه‌ها را بررسی می‌کند.
- یک سؤال با بیشترین کاهش ابهام در هر لحظه پرسیده می‌شود، نه مجموعه‌ای از فیلترهای سنگین.
- اختلاف قیمت با تفاوت محتویات، برند، وضعیت کالا، گارانتی، اصالت ادعایی و شرایط فروش توضیح داده می‌شود.
- اعتماد به فروشنده به‌صورت شواهد جداگانه نمایش داده می‌شود، نه امتیاز واحد و غیرقابل‌توضیح.

### سازگاری عقب‌رو

کالاهای اداری و IT فعلی بدون `decision_context` همچنان مثل قبل نمایش داده می‌شوند. این لایه برای محصولاتی که ابهام تناسب یا تفاوت variant دارند فعال می‌شود؛ بنابراین مسیر RFQ، APIهای فعلی و جدول قیمت حذف یا شکسته نمی‌شوند.

## ۳. مسئله و کاربران

**کاربر اصلی:** خریدار سازمانی که باید برای انتخاب کالا توضیح قابل‌دفاع داشته باشد، نه صرفاً ارزان‌ترین لینک.

**کاربر عمودی قطعات خودرو:** شخصی که جست‌وجو می‌کند «دیسک صفحه ۲۰۶ تیپ ۵» یا «بلبرینگ پلوس ۴۰۵» و نمی‌داند تفاوت کیت، برند، کد، سایز، گارانتی و فروشنده چیست.

**کاربر دسته‌های فعلی:** مسئول خریدی که می‌پرسد کدام تونر به پرینترش می‌خورد، کدام مانیتور با سیستم موجود سازگار است یا چرا دو کاغذ A4 با قیمت متفاوت عرضه شده‌اند.

مسئلهٔ مشترک این است:

> دادهٔ ناقص است، اما کاربر باید تصمیمی بگیرد که هزینه و ریسک واقعی دارد.

## ۴. اهداف و معیارها

### اهداف

1. کاربر در اولین نگاه بفهمد گزینه‌ها به نیاز یا وسیلهٔ او می‌خورند یا نه.
2. کاربر بتواند علت اختلاف قیمت بین گزینه‌های ظاهراً مشابه را توضیح دهد.
3. مقایسهٔ فروشنده بعد از مشخص‌شدن گزینهٔ مناسب انجام شود، نه قبل از آن.
4. هر ادعای سازگاری، کیفیت یا اعتماد با وضعیت و منشأ روشن نمایش داده شود.
5. این رفتار برای verticalهای بعدی بدون بازنویسی مدل اصلی محصول قابل استفاده باشد.

### معیارهای پیشنهادی

- زمان تا اولین اقدام مطمئن کاربر کمتر از ۲۰ ثانیه.
- درصد کاربرانی که بدون برگشت به جست‌وجوی خام، یک گزینهٔ سازگار را انتخاب می‌کنند.
- نرخ پاسخ به سؤال تکمیلی و کاهش ابهام بعد از پاسخ.
- درصد کاربرانی که در آزمون کوتاه، علت اختلاف قیمت دو گزینه را درست تشخیص می‌دهند.
- نرخ گزارش «برای وسیلهٔ من مناسب نبود» یا برگشت ناشی از ناسازگاری.
- نرخ کلیک روی فروشنده همراه با مشاهدهٔ شواهد، نه صرفاً کلیک روی ارزان‌ترین قیمت.

### خارج از هدف

- اعلام قطعی اصالت یا مناسب‌بودن بدون شاهد مستقل.
- ساخت امتیاز اعتماد از دادهٔ ناکافی.
- سفارش، پرداخت، مذاکره یا تماس خودکار.
- ساخت فیلتر برای هر spec نامرتب.
- تبدیل ترب‌بیزینس به تعمیرگاه، نقشهٔ فروشگاه یا فروشگاه مستقل.

## ۵. تجربهٔ پیشنهادی

### ۵.۱ صفحهٔ «جواب کالا»

به‌جای شروع با لیست بلند، نتیجه با یک خلاصه شروع می‌شود:

```text
برای پژو ۲۰۶ تیپ ۵

۳ گزینه با سازگاری بالا
۲ گزینه که باید کد/سایز آن‌ها بررسی شود
۵ گزینه که اطلاعات کافی ندارند
```

وضعیت‌ها:

- **سازگار با شواهد:** اطلاعات خودرو/نیاز و ویژگی محصول هم‌خوان است.
- **احتمالاً سازگار:** بخشی از اطلاعات از عنوان یا اظهار فروشنده آمده است.
- **نیازمند بررسی:** یک ویژگی کلیدی مانند کد، سایز، مدل یا محتویات بسته نامعلوم است.

امتیاز درصدیِ کاذب نمایش داده نمی‌شود.

### ۵.۲ سؤال تکمیلی با بیشترین ارزش

سؤال باید کوچک و قابل‌پاسخ باشد:

```text
دیسک‌وصفحه می‌خواهی یا کیت کامل کلاچ؟
[دیسک‌وصفحه] [کیت کامل] [نمی‌دانم]
```

یا:

```text
کد/سایز بلبرینگ را داری؟
[۶۰۰۶] [۱۳-۵۵×۳۰] [عکس یا کد ندارم]
```

تا وقتی پاسخ لازم نیست، کاربر می‌تواند ادامه دهد؛ سیستم او را در فرم چندمرحله‌ای متوقف نمی‌کند.

### ۵.۳ گروه‌بندی گزینه‌ها

نتایج بر اساس چیزی که واقعاً قابل‌مقایسه است گروه می‌شوند:

- کاربرد و تناسب یکسان
- محتویات یکسان
- وضعیت نو/استوک/کارکرده
- بازهٔ کیفیت یا برند
- گارانتی و شرایط خدمات

بعد از گروه‌بندی، سه مسیر ساده کافی است: `اقتصادی`, `متعادل`, `مستندتر/گارانتی‌دار`.

### ۵.۴ توضیح اختلاف قیمت

هر اختلاف قیمت باید به تفاوت قابل‌مشاهده وصل شود:

```text
گزینهٔ گران‌تر فقط برند دیگری نیست؛
کیت کامل است، گارانتی شرکتی دارد و کد محصول آن مشخص است.
گزینهٔ ارزان‌تر برای همین خودرو معرفی شده، اما اصالت و محتویات کامل آن ثبت نشده است.
```

اگر دلیل در داده نیست، متن باید بگوید «علت اختلاف در اطلاعات ثبت‌شده مشخص نیست».

### ۵.۵ مقایسهٔ فروشنده

بعد از انتخاب گروه/variant، فروشنده‌ها با شواهد مقایسه می‌شوند:

- قیمت و زمان آخرین ثبت
- فاکتور
- گارانتی و مرجوعی
- موجودی یا امکان تأمین
- خرید حضوری/نصب، اگر واقعاً ثبت شده باشد
- سابقهٔ فروش در همان دسته، فقط در صورت وجود داده

نقشه فقط وقتی وارد مسیر اصلی می‌شود که کاربر `فوری`, `نصب`, `تحویل حضوری` یا موقعیت مکانی را مطرح کرده باشد.

## ۶. نیازمندی‌های عملکردی

| شناسه | نیازمندی | معیار پذیرش |
| --- | --- | --- |
| DC-FR-001 | استخراج زمینهٔ تصمیم از query | از عبارت کاربر، دامنه، کالای اصلی و مشخصات قابل‌تشخیص جدا می‌شود؛ مقدار نامعلوم ساخته نمی‌شود. |
| DC-FR-002 | ثبت ادعای ویژگی با شواهد | هر claim وضعیت `confirmed`, `seller_claimed`, `inferred` یا `unknown` و منبع/زمان دارد. |
| DC-FR-003 | سؤال تکمیلی کمینه | در هر گام حداکثر یک سؤال اصلی نمایش داده می‌شود و گزینهٔ «نمی‌دانم» وجود دارد. |
| DC-FR-004 | گروه‌بندی variantهای قابل‌مقایسه | محصولاتی که محتویات، کد یا تناسب متفاوت دارند در یک گزینهٔ هم‌ارز ادغام نمی‌شوند. |
| DC-FR-005 | توضیح اختلاف قیمت | برای هر دو گزینه، تفاوت‌های داده‌شده در ویژگی، محتویات یا شرایط معامله نمایش داده می‌شود. |
| DC-FR-006 | لایهٔ شواهد اعتماد فروشنده | اعتماد به تفکیک ادعا/منبع/تازگی نمایش داده می‌شود؛ امتیاز کلی بدون دادهٔ کافی تولید نمی‌شود. |
| DC-FR-007 | نمایش وضعیت ابهام | گزینه‌های نامطمئن از گزینه‌های تأییدشده جدا هستند و پیش‌فرض خرید قطعی نمی‌سازند. |
| DC-FR-008 | اتصال به قیمت فعلی | هر decision option به همان `product_id`, `supplier_id` و `product_price` موجود متصل می‌شود؛ قیمت جدید موازی و بدون منشأ ساخته نمی‌شود. |
| DC-FR-009 | سازگاری با RFQ | کاربر می‌تواند گزینهٔ انتخاب‌شده را به RFQ فعلی اضافه کند؛ decision context و پاسخ‌هایش همراه RFQ می‌رود. |
| DC-FR-010 | منشأ قابل‌مشاهده | از تصمیم نهایی تا claim، قیمت و رکورد خام مسیر قابل‌مشاهده است. |

## ۷. مدل دادهٔ پیشنهادی

جداول فعلی باقی می‌مانند و این موجودیت‌ها به‌صورت اختیاری اضافه می‌شوند:

| موجودیت | فیلدهای کلیدی | نقش |
| --- | --- | --- |
| `decision_contexts` | `id`, `domain`, `query_text`, `subject_type`, `subject_fields`, `status` | زمینهٔ تصمیم مثل خودرو یا مدل دستگاه |
| `product_variants` | `product_id`, `family_id`, `variant_fields`, `contents`, `condition` | هویت قابل‌مقایسهٔ کالا |
| `fitment_claims` | `context_id`, `product_id`, `field`, `value`, `status`, `evidence_ref`, `observed_at` | ادعای سازگاری یا تناسب |
| `clarifying_questions` | `context_id`, `field`, `options`, `information_gain`, `answer` | سؤال کوتاه برای کم‌کردن ابهام |
| `supplier_evidence` | `supplier_id`, `product_id`, `signal`, `status`, `source_ref`, `observed_at` | شواهد فروشنده و کیفیت معامله |
| `decision_explanations` | `context_id`, `product_id`, `reason_codes`, `compared_fields` | توضیح ساختاریافتهٔ انتخاب و اختلاف قیمت |

برای قطعات خودرو، `subject_fields` می‌تواند شامل سازنده، مدل، تیپ، موتور و سال باشد. برای تجهیزات IT، همین الگو برای مدل دستگاه، رابط، ظرفیت و نیاز سازمانی استفاده می‌شود.

## ۸. قرارداد API پیشنهادی

APIهای فعلی حفظ می‌شوند. این endpointها روی آن‌ها سوار می‌شوند:

- `GET /api/catalog/decision?q=...` — استخراج زمینه، گزینه‌های اولیه و ابهام‌ها.
- `POST /api/catalog/decision/{id}/answers` — ثبت پاسخ یک سؤال و بازسازی گزینه‌ها.
- `GET /api/catalog/decision/{id}` — گزینه‌های گروه‌بندی‌شده، دلیل تفاوت قیمت و شواهد.
- `GET /api/catalog/decision/{id}/provenance` — مسیر ادعاها تا منبع خام.
- `POST /api/rfqs` — پذیرش `decision_context_id` و `selected_variant_id` به‌صورت اختیاری.

اگر دادهٔ سازگاری موجود نباشد، `GET /api/catalog/decision` باید همچنان لیست قیمت فعلی را برگرداند، اما با وضعیت `unknown_fitment`؛ شکست دادهٔ تصمیم نباید ویترین فعلی را از کار بیندازد.

## ۹. تحویل مرحله‌ای

### فاز ۱ — قابل‌استفاده برای پروژهٔ فعلی

- اضافه‌کردن status و source به specها و claimهای محصول.
- نمایش «تأییدشده / اظهار فروشنده / نامشخص» در جزئیات کالا.
- گروه‌بندی سادهٔ گزینه‌ها بر اساس دسته، برند، مدل، محتویات و وضعیت.
- نمایش توضیح اختلاف قیمت برای همان دادهٔ موجود.
- اتصال گزینهٔ انتخاب‌شده به RFQ فعلی.

### فاز ۲ — vertical قطعات خودرو

- ساخت fixture کوچک برای ۲۰۶ تیپ ۵ و ۴۰۵.
- مدل‌کردن کد/سایز، کیت یا قطعهٔ تکی، برند و گارانتی.
- اضافه‌کردن سؤال‌های کمینه و مسیر «عکس یا کد ندارم».
- ارزیابی با سناریوهای واقعیِ عنوان‌های نامرتب و قیمت‌های ناهم‌ارز.

### فاز ۳ — فروشنده و نقشه

- شواهد فروشنده و freshness قیمت.
- نمایش فروش حضوری یا نصب فقط با دادهٔ معتبر.
- نقشه به‌عنوان حالت خرید فوری، نه هستهٔ نتایج.

## ۱۰. ریسک‌ها و تصمیم‌های باز

- **ریسک ایمنی/اعتماد:** اعلام قطعی سازگاری قطعهٔ خودرو می‌تواند زیان واقعی داشته باشد؛ وضعیت نامعلوم و شواهد باید اجباری بماند.
- **ریسک داده:** عنوان فروشنده منبع کافی برای اصالت یا تناسب نیست.
- **ریسک مقایسه:** «کیت» و «قطعه» نباید در یک رتبه‌بندی قیمت قرار بگیرند.
- **ریسک دامنه:** vertical خودرو نباید دسته‌های اداری و IT را از پروژهٔ اصلی بیرون بزند.
- **تصمیم باز:** اولین vertical واقعی بعد از دسته‌های فعلی خودرو باشد یا تونر/قطعات مصرفی سازمانی.
- **تصمیم باز:** قبل از نمایش شواهد فروشنده، چه منابعی قابل‌قبول و قابل‌ثبت هستند.
- **تصمیم باز:** معیار ارزیابی سازگاری با کارشناس، فروشنده، دادهٔ تولیدکننده یا تست کاربر چگونه ترکیب شود.

## ۱۱. معیار پذیرش و تعریف انجام‌شدن

این delta زمانی آماده است که:

1. یک query مبهم حداقل یک سؤال تکمیلی و گزینهٔ «نمی‌دانم» داشته باشد.
2. دو محصول با قیمت متفاوت، تفاوت محتویات/برند/گارانتی یا وضعیت را جداگانه نشان دهند.
3. محصولی با claim نامعلوم هرگز با برچسب «سازگار قطعی» نمایش داده نشود.
4. قیمت، فروشنده و وضعیت اعتماد از جدول‌های فعلی و منشأ ثبت‌شده بیاید.
5. گزینهٔ انتخاب‌شده بدون از دست‌دادن context وارد RFQ موجود شود.
6. نبود دادهٔ سازگاری، ویترین فعلی را خراب نکند و با پیام روشن نمایش داده شود.
7. برای سناریوهای «دیسک صفحه ۲۰۶ تیپ ۵» و «بلبرینگ پلوس ۴۰۵»، کاربر بتواند در یک مسیر کوتاه بین گزینهٔ اقتصادی، متعادل و نیازمند بررسی فرق بگذارد.
